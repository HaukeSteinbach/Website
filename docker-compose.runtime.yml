services:
  app:
    image: ghcr.io/haukesteinbach/haukesteinbach:${IMAGE_TAG:?set IMAGE_TAG in .env}
    pull_policy: always
    container_name: steinbachapp
    restart: unless-stopped
    ports:
      - "${HOST_PORT:-80}:80"
    env_file:
      - ./backend/.env.runtime
    volumes:
      - uploads-data:/var/lib/steinbach/uploads

volumes:
  uploads-data: